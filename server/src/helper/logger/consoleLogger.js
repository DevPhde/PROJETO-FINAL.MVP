export class Logger {
    static console(param){
        console.log(
`------------------------------------------------------------------------
${param}
------------------------------------------------------------------------`);
    }
}